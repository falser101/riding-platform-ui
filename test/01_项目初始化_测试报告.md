# 测试报告 - 项目初始化

## 1. 测试概述

| 字段 | 内容 |
|------|------|
| 需求编号 | REQ-001 |
| 需求名称 | 项目初始化 |
| 测试类型 | 冒烟测试 + 功能测试 |
| 测试日期 | 2024-XX-XX |
| 测试人员 | QA Agent |

---

## 2. 测试范围

### 2.1 后台管理系统 (Admin)

| 测试对象 | 测试内容 |
|----------|----------|
| 项目启动 | dev server 启动、端口监听、热更新 |
| 页面渲染 | Element Plus 组件渲染、样式加载 |
| 路由功能 | 页面跳转、路由守卫 |
| 用户登录 | 登录流程、Token 处理 |

### 2.2 H5 前端

| 测试对象 | 测试内容 |
|----------|----------|
| 项目启动 | dev server 启动、端口监听 |
| 页面渲染 | Vant 组件渲染、移动端适配 |
| TabBar | 底部导航切换 |
| 登录功能 | 手机号登录、验证码 |

---

## 3. 测试用例

### 3.1 后台管理系统 - 项目启动

#### TC-001: Admin 项目正常启动

| 字段 | 内容 |
|------|------|
| 用例ID | TC-001 |
| 用例名称 | Admin 项目正常启动 |
| 优先级 | P0 |
| 前置条件 | 已执行 `cd ~/projects/riding-platform/admin && npm install && npm run dev` |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 执行 `npm run dev` 命令 | 命令执行成功，无报错 |
| 2 | 等待 5-10 秒 | 终端显示 `Local: http://localhost:5173/` |
| 3 | 打开 Chrome 浏览器 | 浏览器正常打开 |
| 4 | 访问 `http://localhost:5173` | 页面加载，显示后台登录页或首页 |

**测试数据:** 无

**预期结果:**
- Vite dev server 启动成功
- 服务监听端口 5173
- 页面无白屏，正常渲染 Element Plus 组件

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

#### TC-002: Element Plus 组件渲染验证

| 字段 | 内容 |
|------|------|
| 用例ID | TC-002 |
| 用例名称 | Element Plus 组件渲染验证 |
| 优先级 | P0 |
| 前置条件 | Admin 项目已启动 |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 访问 `http://localhost:5173` | 显示登录页面 |
| 2 | 输入手机号: `13800138000` | 输入框显示对应数字 |
| 3 | 输入验证码: `123456` | 输入框显示对应数字 |
| 4 | 点击"登录"按钮 | 按钮显示 loading 状态 |
| 5 | 等待 2 秒 | 登录成功，跳转到首页或后台管理页面 |

**测试数据:**
- 手机号: `13800138000`
- 验证码: `123456`

**预期结果:**
- Element Plus Input/Button 组件正常渲染
- 按钮点击有 hover/active 效果
- 表单提交有 loading 状态反馈

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

#### TC-003: 路由守卫功能验证

| 字段 | 内容 |
|------|------|
| 用例ID | TC-003 |
| 用例名称 | 路由守卫功能验证 |
| 优先级 | P1 |
| 前置条件 | Admin 项目已启动，用户已登录 |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 清除浏览器 localStorage 中的 token | Token 已清除 |
| 2 | 刷新页面或访问 `http://localhost:5173/dashboard` | 自动跳转到登录页 `/login` |
| 3 | 登录成功后访问 `/dashboard` | 正常显示后台页面，不跳转 |

**测试数据:** 无

**预期结果:**
- 未登录状态下直接访问受保护路由，自动跳转到 `/login`
- URL 地址栏变化为登录页地址
- 登录成功后可正常访问受保护页面

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

#### TC-004: Token 持久化验证

| 字段 | 内容 |
|------|------|
| 用例ID | TC-004 |
| 用例名称 | Token 持久化验证 |
| 优先级 | P1 |
| 前置条件 | Admin 项目已启动 |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 登录后台系统 | 登录成功，Token 已存储 |
| 2 | 打开 Chrome DevTools -> Application -> Local Storage | 存在 `admin_token` 字段 |
| 3 | 关闭浏览器标签页 | 浏览器关闭 |
| 4 | 重新打开浏览器，访问后台地址 | 页面显示已登录状态（无需重新登录） |

**测试数据:** 无

**预期结果:**
- Token 以 `admin_token` 为 key 存储在 localStorage
- Token 值非空（JWT 格式字符串）
- 关闭并重新打开浏览器后，仍处于登录状态

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

### 3.2 H5 前端 - 项目启动

#### TC-005: H5 项目正常启动

| 字段 | 内容 |
|------|------|
| 用例ID | TC-005 |
| 用例名称 | H5 项目正常启动 |
| 优先级 | P0 |
| 前置条件 | 已执行 `cd ~/projects/riding-platform/h5 && npm install && npm run dev` |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 执行 `npm run dev` 命令 | 命令执行成功，无报错 |
| 2 | 等待 5-10 秒 | 终端显示 `Local: http://localhost:5174/` |
| 3 | 使用 Chrome 移动端模拟器（iPhone 12 Pro） | 模拟器正常显示 |
| 4 | 访问 `http://localhost:5174` | 页面加载，显示首页或 TabBar |

**测试数据:** 无

**预期结果:**
- Vite dev server 启动成功
- 服务监听端口 5174
- 页面在移动端模拟器下正常显示

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

#### TC-006: Vant 组件渲染验证

| 字段 | 内容 |
|------|------|
| 用例ID | TC-006 |
| 用例名称 | Vant 组件渲染验证 |
| 优先级 | P0 |
| 前置条件 | H5 项目已启动 |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 访问 `http://localhost:5174` | 显示首页或底部 TabBar |
| 2 | 点击底部"我的"或"个人中心" Tab | 页面切换到个人中心 |
| 3 | 点击"登录"按钮 | 跳转到登录页 |
| 4 | 输入手机号: `13800138000` | 输入框显示数字 |
| 5 | 点击"获取验证码"按钮 | 按钮显示倒计时 "59s" |
| 6 | 输入验证码: `123456` | 输入框显示数字 |

**测试数据:**
- 手机号: `13800138000`
- 验证码: `123456`

**预期结果:**
- Vant Button/Field/TabBar 组件正常渲染
- 移动端手势正常（触摸滚动、点击反馈）
- 验证码倒计时功能正常

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

#### TC-007: TabBar 导航切换验证

| 字段 | 内容 |
|------|------|
| 用例ID | TC-007 |
| 用例名称 | TabBar 导航切换验证 |
| 优先级 | P1 |
| 前置条件 | H5 项目已启动 |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 访问 `http://localhost:5174` | 显示首页 Tab 选中状态 |
| 2 | 点击底部 TabBar "活动" 按钮 | 页面切换到活动列表页 |
| 3 | 观察 TabBar | "活动" Tab 高亮显示 |
| 4 | 点击底部 TabBar "我的" 按钮 | 页面切换到个人中心 |
| 5 | 点击底部 TabBar "首页" 按钮 | 页面切换回首页 |

**测试数据:** 无

**预期结果:**
- TabBar 始终固定在页面底部
- Tab 切换时内容区域平滑过渡
- 当前选中 Tab 有视觉区分（颜色/图标变化）
- 页面切换后 URL 变化（如: `/pages/index`、`/pages/profile`）

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

#### TC-008: 小程序页面导航模拟验证

| 字段 | 内容 |
|------|------|
| 用例ID | TC-008 |
| 用例名称 | 小程序页面导航模拟验证 |
| 优先级 | P1 |
| 前置条件 | H5 项目已启动 |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在个人中心页面点击登录 | 跳转至登录页 |
| 2 | 输入手机号和验证码，点击登录 | 登录成功后跳转到个人中心（而非首页） |
| 3 | 在个人中心点击"我的活动" | navigateTo 到活动详情页 |
| 4 | 点击页面左上角返回按钮 | navigateBack 返回到个人中心 |

**测试数据:**
- 手机号: `13800138000`
- 验证码: `123456`

**预期结果:**
- `wx.navigateTo` 正确压入页面栈，URL 更新
- `wx.navigateBack` 正确弹出页面栈，返回上一页
- 页面切换动画符合小程序体验（横向滑动）

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

### 3.3 Mock API 测试

#### TC-009: 验证码发送接口验证

| 字段 | 内容 |
|------|------|
| 用例ID | TC-009 |
| 用例名称 | 验证码发送接口验证 |
| 优先级 | P0 |
| 前置条件 | json-server 已启动（端口 3001） |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开 Postman 或 curl | 工具正常打开 |
| 2 | 发送 POST 请求: `http://localhost:3001/api/auth/send-code` | 请求发送成功 |
| 3 | Body 设置为 raw JSON: `{"phone":"13800138000"}` | 请求体正确设置 |
| 4 | 查看响应 | 返回验证码（如 `"data": {"验证码": "123456"}`） |

**测试数据:**
```json
{
  "phone": "13800138000"
}
```

**预期结果:**
- HTTP 状态码: 200
- 响应格式: `{"code": 0, "message": "success", "data": {"验证码": "123456"}}`

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

#### TC-010: 用户登录接口验证

| 字段 | 内容 |
|------|------|
| 用例ID | TC-010 |
| 用例名称 | 用户登录接口验证 |
| 优先级 | P0 |
| 前置条件 | json-server 已启动（端口 3001） |

**测试步骤:**

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 发送 POST 请求: `http://localhost:3001/api/auth/login` | 请求发送成功 |
| 2 | Body: `{"phone":"13800138000","code":"123456","type":"h5"}` | 请求体正确设置 |
| 3 | 查看响应 | 返回 token 和 userInfo |

**测试数据:**
```json
{
  "phone": "13800138000",
  "code": "123456",
  "type": "h5"
}
```

**预期结果:**
- HTTP 状态码: 200
- 响应包含: `token` (JWT 格式)、`userInfo` (包含 id/phone/nickname/role)
- code 为 0

**实际结果:** 待填写

**结论:** ☐ 通过 ☐ 失败 ☐ 阻塞

---

## 4. 测试结果汇总

### 4.1 执行统计

| 指标 | 数值 |
|------|------|
| 用例总数 | 10 |
| 通过数 | 待填写 |
| 失败数 | 待填写 |
| 阻塞数 | 待填写 |
| 通过率 | 待填写 |

### 4.2 缺陷列表

| 缺陷ID | 用例ID | 缺陷描述 | 严重程度 | 状态 |
|--------|--------|----------|----------|------|
| - | - | - | - | - |

---

## 5. 测试环境

| 环境项 | 配置 |
|--------|------|
| 操作系统 | macOS / Windows |
| 浏览器 | Chrome 90+ |
| Node.js | 18.0+ |
| Mock Server | json-server@0.17 |
| 移动端模拟 | Chrome DevTools Device Mode |

---

## 6. 风险评估

| 风险项 | 影响 | 缓解措施 |
|--------|------|----------|
| 跨域问题 | API 请求失败 | dev server 配置 proxy |
| CORS 错误 | 前端无法请求后端 | Mock server 配置 no-cors |
| 移动端兼容 | 部分组件显示异常 | 使用 Vant 移动端专用组件 |

---

## 7. 验收标准

所有 P0 用例通过，且无阻塞级缺陷，方可验收通过。

**验收签字:**
- PM: __________ 日期: __________
- Dev: __________ 日期: __________
- QA: __________ 日期: __________
